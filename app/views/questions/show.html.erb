<div class="container">
  <h2 class="title-lg mb">Вопрос <%= @question.id %> </h2>
  <p class="mt-sm"><%= link_to "Все вопросы #{nickname_with_dog(@question.user)}", user_path(@question.user) %></p>
</div>

<div class="container mt-sm">
  <%= render 'question', question: @question %>
</div>
